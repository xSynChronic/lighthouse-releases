# Lighthouse releases

Installers for Lighthouse, a practice app for one Washington broker, notary and
transaction coordinator. This repository holds release files only; the code is
private.

- Mac: `Lighthouse-mac.dmg` (Apple silicon and Intel)
- Windows: `Lighthouse-Setup.exe`

The app checks this repository's latest release for updates. Nothing of the
user's is ever sent here.
